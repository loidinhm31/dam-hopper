{
  "name": "dam-hopper",
  "private": true,
  "type": "module",
  "scripts": {
    "android:init": "npm --prefix apps/native run tauri:android:init",
    "android:dev": "npm --prefix apps/native run tauri:android:dev",
    "android:build": "npm --prefix apps/native run tauri:android:build",
    "android:run": "npm --prefix apps/native run tauri:android:run",
    "android:studio": "npm --prefix apps/native run tauri:android:studio",
    "build": "pnpm --filter @dam-hopper/web build",
    "build:native": "pnpm --filter @dam-hopper/native build",
    "dev": "pnpm --filter @dam-hopper/web dev --host",
    "dev:native": "pnpm --filter @dam-hopper/native tauri:dev",
    "dev:server:no-auth": "cd server && cargo run -- --no-auth",
    "dev:server": "cd server && RUST_LOG=debug cargo run",
    "dev:server:watch": "cd server && cargo watch -x run",
    "build:server": "cd server && cargo build --release",
    "server:install": "deploy/run-linux-nohup.sh install --bin server/target/release/dam-hopper-server",
    "server:start": "deploy/run-linux-nohup.sh start",
    "server:stop": "deploy/run-linux-nohup.sh stop",
    "server:restart": "deploy/run-linux-nohup.sh restart --bin server/target/release/dam-hopper-server",
    "server:status": "deploy/run-linux-nohup.sh status",
    "serve": "cd server && cargo run --release",
    "lint": "eslint apps/ packages/",
    "format": "prettier --write .",
    "test": "cd server && cargo test",
    "test:run": "cd server && cargo test",
    "check": "pnpm build && pnpm build:native && pnpm lint && cd server && cargo test"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@eslint/compat": "^2.0.5",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^7.0.1",
    "prettier": "^3.0.0"
  }
}
